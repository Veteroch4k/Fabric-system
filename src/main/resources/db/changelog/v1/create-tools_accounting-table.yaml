databaseChangeLog:
  - changeSet:
      id: 13
      author: Veteroch4k
      changes:
        - createTable:
            tableName: tools-accounting
            columns:
              - column:
                  constraints:
                    nullable: false
                    primaryKey: true
                  autoIncrement: true
                  type: INT
                  name: id
              - column:
                  constraints:
                    nullable: false
                    foreignKey: true
                    references: tools(id)
                  type: INT
                  name: tool_id
              - column:
                  constraints:
                    nullable: false
                    foreignKey: true
                    references: tool_types(id)
                  type: INT
                  name: tool_type_id
              - column:
                  constraints:
                    nullable: false
                    foreignKey: true
                    references: factories(id)
                  type: INT
                  name: factory_id
              - column:
                  constraints:
                    nullable: false
                    foreignKey: true
                    references: orders(id)
                  type: INT
                  name: order_id